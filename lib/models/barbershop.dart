import 'package:cloud_firestore/cloud_firestore.dart';
/*
class BarberShop {
  String name;
  String description;
  String imageUrl;
  int rating;
  DocumentReference reference;

  BarberShop({
    this.name,
    this.description,
    this.imageUrl,
    this.rating,
    this.reference,
  });

  BarberShop.fromMap(Map<String, dynamic> map, {this.reference}) {
    name = map['name'];
    description = map['description'];
    imageUrl = map['imageUrl'];
    rating = map['rating'];
  }

  BarberShop.fromSnapshot(DocumentSnapshot snapshot)
      : this.fromMap(snapshot.data(), reference: snapshot.reference);

  Map<String, dynamic> toMap() {
    return {
      'name': name,
      'description': description,
      'imageUrl': imageUrl,
      'rating': rating,
    };
  }
}
*/
